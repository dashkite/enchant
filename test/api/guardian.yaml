resources:

  description:
    template: /
    methods:
      get:
        signatures:
          request: {}
          response:
            content-type: [ "application/json" ]
            status: [ 200 ]

  authenticate:
    template: /authenticate
    methods:
      post:
        signatures:
          request:
            content-type: [ "text/plain"]
          response:
            content-type: [ "application/json" ]
            status: [ 200 ]
