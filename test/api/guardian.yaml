resources:

  description:
    template: /
    methods:
      get:
        signatures:
          request: {}
          response:
            content-type: [ "application/json" ]
            status: [ 200 ]

  authenticate:
    template: /authenticate/{ciphertext}
    methods:
      get:
        signatures:
          request: {}
          response:
            content-type: [ "application/json" ]
            status: [ 200 ]
